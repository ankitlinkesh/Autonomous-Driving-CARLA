# Models

Place downloaded model weights here locally. Model binaries are ignored by Git.

The default configuration uses `models/yolov8n.pt`; Ultralytics downloads this checkpoint on first use when network access is available. For academic reproducibility, record the model name, version, dataset, and checksum in experiment notes.

