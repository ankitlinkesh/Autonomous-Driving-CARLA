# Notebooks

Use this directory for exploratory analysis and experiment notebooks. Keep reusable production logic in `src/` and make notebooks call those modules rather than duplicating implementations.

